name: C++ Auto Tests

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Compile Test
      run: g++ auto_test.cpp -o run_test
    - name: Run Test
      run: ./run_test
