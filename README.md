# lab1_PT

1. Create a Github repository (PUBLIC!!!).
2. Add ssh key to your account
3. Write a dumb code on some programming language (python, js, etc.)
4. Commit code to repository
5. Add auto-tests (read more in Internet)
6. Add small pipeline (lint, test, export results, build/push to registry optional)
